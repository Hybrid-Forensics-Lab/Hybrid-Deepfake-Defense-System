# Backend deps (installed into the webapp-env conda environment).
# Torch + CUDA come from the cu121 index; pinned to match the rest of the project.
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart
pydantic==2.13.4
pillow
numpy<2.0
scikit-learn==1.3.2
scipy==1.11.4
joblib==1.5.3
open-clip-torch==3.3.0
facenet-pytorch==2.6.0
scikit-image
ftfy
regex
tqdm
# torch==2.1.0+cu121 / torchvision==0.16.0+cu121 installed separately via the cu121 index
