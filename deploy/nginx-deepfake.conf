server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    root /var/www/deepfake;
    index index.html;
    client_max_body_size 12M;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
